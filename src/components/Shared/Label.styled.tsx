import styled from "styled-components"
import { regular } from "styles/fonts"
import { x1 } from "styles/sizes"

export const Label = styled.label`
  font-size: ${regular};
  padding-bottom: ${x1};
`
